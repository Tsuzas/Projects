int firstValue = 500;
int secondValue = 600;
int largerValue;

largerValue = Math.Max(firstValue, secondValue);    // era isto a resposta, google worrthy but ez to find

Console.WriteLine(largerValue);
